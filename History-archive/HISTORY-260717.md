# 作業履歴

このファイルは、日ごとの作業状況を時次進捗形式で記録するためのものです。

## 2026-07-17

### 12時進捗

- 作業開始時に `AGENTS.md` の指示と `HISTORY.md` を確認し、前回分が 2026-07-15 の履歴であることを確認した。
- 日付が今日 2026-07-17 と異なるため、既存の `HISTORY.md` を `History-archive/HISTORY-260715.md` にアーカイブした。
- 今日分の新しい `HISTORY.md` を作成し、2026-07-17 の見出しから記録を開始した。
- 今回は履歴の移行のみを行い、サイト本体、SCSS、CSS、JavaScript、HTMLには変更を加えていない。
- `ROADMAP.md` のフェーズ2・ステップ2-4を確認し、料金表ページだけを対象にレスポンシブ対応を開始した。`price.html`、`scss/style.scss`、生成CSS、`js/main.js`、`package.json`、`README.md`、表示確認用設定・スクリプトを再確認した。
- `scss/style.scss` にPriceページ専用のレスポンシブ指定を追加した。1024px以下では固定600pxだったアンカーリンク2枚を可変2列にし、料金表の文字サイズ、caption、セル余白を段階的に調整した。
- 768px以下ではアンカーリンクを1列へ変更し、料金表を最小幅720pxのまま `.table-wrap` 内だけ横スクロールできる構成にした。captionの補足文はブロック化し、折り返しやすくした。
- 430px以下では `mv-PriceList_SP.png` のヒーローへ切り替え、アンカーリンクを最大386px、料金表を16px・最小幅620pxへ調整した。紹介文も16pxへ縮小した。
- 既存の `tools/build-css.ps1` を実行し、`css/style.css`、`scss/style.css`、`scss/style.min.css` と各マップを同期した。再ビルド後も生成物のハッシュが変わらず、ビルド結果が安定していることを確認した。
- Priceページの1600pxスクリーンショットを生成し、変更前基準と画像ハッシュが一致してPC版に差分がないことを確認した。1599px、1024px、1023px、768px、767pxでもスクリーンショットを生成し、アンカーリンク、料金表、caption、フッターまで目視確認した。
- 768px以下では料金表に必要な横スクロールバーが表示され、ページ幅ではなく表の中だけがスクロール対象になることを確認した。
- Windows版ヘッドレスChromeは500px未満のレイアウト幅を直接再現できなかったため、430pxと429pxは生成CSSのメディアクエリ、SPヒーロー画像の存在、コンテナ算出幅、表内オーバーフロー条件を機械的に確認した。アンカーリンクは画面内に収まり、料金表だけが横スクロール対象になる値であることを確認した。
- `ROADMAP.md` のステップ2-4を完了へ更新し、進行状況チェックのPriceを完了にした。次のステップ2-5には進んでいない。

### 14時進捗

- `ROADMAP.md` のフェーズ2・ステップ2-5を確認し、FAQページだけを対象にレスポンシブ対応を行った。`faq.html`、FAQ関連SCSS、既存の開閉処理、表示確認手順を確認し、HTMLの文章とJavaScriptは変更していない。
- `scss/style.scss` にFAQページ専用のレスポンシブ指定を追加した。1600px以下はカテゴリカード4列をコンテンツ幅内に収め、1024px以下は2列、768px以下は1列へ変更した。補足リストの行間とFAQグループ間の余白も段階的に調整した。
- 1024px、768px、430pxの各段階でFAQ本文の最大幅、見出し、質問・回答の文字サイズと余白を調整した。長い質問文を自然に折り返し、開閉アイコンが縮まず文字と重ならないようにした。
- 430px以下では `mv-faq_SP.png` のヒーローへ切り替え、複数行の質問・回答に合わせて `Q.`、`A.`、開閉アイコンの位置を調整した。
- 既存のCSSビルドスクリプトを実行し、`css/style.css`、`scss/style.css`、`scss/style.min.css` と各マップを同期した。再ビルド後も全生成物のハッシュが変わらず、マップも正常に読み込めることを確認した。
- 1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxを実幅で表示確認した。全13件を展開しても質問・回答・カテゴリカードの横はみ出しとページ全体の横スクロールはなく、430pxと429pxではSPヒーローが適用された。
- FAQはクリック、Enter、Spaceの各操作で1回ずつ開き、回答表示と `aria-expanded` が同期することを確認した。13個の `aria-controls` はすべて対応する回答IDへ一意に関連付けられていた。
- 1920pxの変更前後スクリーンショットは画像ハッシュが一致し、1600pxより広いPC版に意図しない差分がないことを確認した。
- `ROADMAP.md` のステップ2-5を完了へ更新し、進行状況チェックのFAQを完了にした。次のステップ2-6には進んでいない。
- `ROADMAP.md` のフェーズ2・ステップ2-6を確認し、Contactページだけを対象にレスポンシブ対応を行った。`contact.html`、Contact関連SCSS、既存フォーム処理、表示確認手順を確認し、HTMLの文章とJavaScriptは変更していない。
- `scss/style.scss` にContactページ専用のレスポンシブ指定を追加した。1024px以下では説明文、電話案内、FAQ導線、フォームを段階的に縮小し、768px以下では主要ブロックを最大560pxへ整理した。
- 430px以下では `mv-Contact_SP.png` のヒーローへ切り替え、説明文、電話案内、FAQ導線、フォームを最大386pxに収めた。入力欄、同意欄、送信ボタン、状態メッセージの文字サイズと余白もスマートフォン向けに調整した。
- 既存のCSSビルドスクリプトを実行し、`css/style.css`、`scss/style.css`、`scss/style.min.css` と各マップを同期した。再ビルド後も全生成物のハッシュが変わらず、マップも正常に読み込めることを確認した。
- 1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxを実幅で表示確認した。すべての幅で主要ブロックの横はみ出しとページ全体の横スクロールはなく、430pxと429pxではSPヒーローが適用された。
- 必須項目を入力したフォーム送信で既存の確認メッセージが表示され、入力内容とチェックボックスがリセットされることを確認した。FAQ・プライバシーポリシーへのリンクとライブリージョンも維持されている。
- 1600pxの変更後スクリーンショットは既存基準画像とハッシュが一致し、PC版に意図しない差分がないことを確認した。
- `ROADMAP.md` のステップ2-6を完了へ更新し、進行状況チェックのContactを完了にした。次のステップ2-7には進んでいない。
- `ROADMAP.md` のフェーズ2・ステップ2-7を確認し、Archiveページだけを対象にレスポンシブ対応を行った。`archive.html`、Archive関連SCSS、既存のカテゴリ・月別絞り込み処理、表示確認手順を確認し、HTMLの記事内容とJavaScriptは変更していない。
- `scss/style.scss` にArchiveページ専用のレスポンシブ指定を追加した。1024px以下ではフィルターを2列で維持しながら一覧の列幅を調整し、768px以下ではフィルターを1列、一覧を日付・タグの行とタイトルの行に分けた。
- 430px以下ではフィルターのラベルと選択欄を縦並びにし、一覧項目を日付、タグ、タイトルの順に3段で配置した。一覧を最大386pxに収め、ページネーションは44pxの操作領域を維持した。
- 既存のCSSビルドスクリプトを実行し、`css/style.css`、`scss/style.css`、`scss/style.min.css` と各マップを同期した。再ビルド後も全生成物のハッシュが変わらず、マップも正常に読み込めることを確認した。
- 1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxを実幅で表示確認した。すべての幅でフィルター、全10件の一覧、ページネーションの横はみ出しとページ全体の横スクロールはなかった。
- カテゴリ絞り込みはニュース8件・ブログ2件、月別絞り込みは2026年5月0件、ブログと2026年6月の組み合わせは2件となり、結果表示と実際の表示項目数が一致することを確認した。
- 新規スタイルは1024px以下だけに適用され、1600pxと1599pxでは既存の一覧幅・文字サイズを維持している。既存基準画像との目視比較でも配置差はなかったが、撮影環境の描画差により画像全体のハッシュは一致しなかった。
- `ROADMAP.md` のステップ2-7を完了へ更新し、進行状況チェックのArchiveを完了にした。次のステップ2-8には進んでいない。
- `ROADMAP.md` のフェーズ2・ステップ2-8を確認し、Singleページだけを対象にレスポンシブ対応を行った。`single.html`、Single関連SCSS、本文・サイドバーの構造、各リンク先、表示確認手順を確認し、HTMLの本文とJavaScriptは変更していない。
- `scss/style.scss` にSingleページ専用のレスポンシブ指定を追加した。1024px以下ではサイドバーを300px、列間隔を40pxに調整して本文幅を確保し、見出し、メタ情報、本文、戻りリンク、最新投稿の文字サイズと余白も調整した。
- 768px以下では2列構成を1列へ変更し、DOM順のまま記事本文を先、サイドバーを後に配置した。記事を最大640px、サイドバーを最大560px、バナーを400px以内に収めた。
- 430px以下では記事とサイドバーを最大386pxに収め、見出し24px、本文16px、最新投稿15pxへ調整した。長い見出し・本文・最新投稿タイトルも安全に折り返せるようにした。
- 既存のCSSビルドスクリプトを実行し、`css/style.css`、`scss/style.css`、`scss/style.min.css` と各マップを同期した。再ビルド後も全生成物のハッシュが変わらず、マップも正常に読み込めることを確認した。
- 1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxを実幅で表示確認した。すべての幅で記事、サイドバー、バナー、最新投稿の横はみ出しとページ全体の横スクロールはなく、768px以下ではサイドバーが記事の後に配置された。
- お知らせ一覧への戻りリンク、施設紹介・FAQのバナーリンク、最新投稿3件のリンク先が維持されていることを確認した。
- 新規スタイルは1024px以下だけに適用され、1600pxと1920pxでは本文・サイドバーの既存2列構成を維持している。既存基準画像との目視比較でも配置差はなかったが、撮影環境の描画差により画像全体のハッシュは一致しなかった。
- `ROADMAP.md` のステップ2-8を完了へ更新し、進行状況チェックのSingleを完了にした。次のステップ2-9には進んでいない。
- `ROADMAP.md` のフェーズ2・ステップ2-9を確認し、Privacyページだけを対象にレスポンシブ対応を行った。`privacy.html`、Privacy関連SCSS、長文、入れ子リスト、お問い合わせ先表記、表示確認手順を確認し、HTMLの文章・既存クラスとJavaScriptは変更していない。
- `scss/style.scss` にPrivacyページ専用のレスポンシブ指定を追加した。1024px以下では本文を最大900px、本文文字を20pxにし、見出し、段落、リストの行間と余白を調整した。
- 768px以下では本文を最大640px、本文文字を18pxにし、入れ子リストの左余白を縮小した。長いページタイトルは文字間隔と折り返しを調整し、1行で読みやすく表示した。
- 430px以下では本文を最大386px、本文文字を16px、左右の内側余白を16pxにした。長いタイトル、住所、電話番号、お問い合わせ先、2階層のリストが画面幅内で安全に折り返すようにした。
- 既存のCSSビルドスクリプトを実行し、`css/style.css`、`scss/style.css`、`scss/style.min.css` と各マップを同期した。再ビルド後も5個の生成物のハッシュが変わらず、マップも正常に読み込めることを確認した。
- 1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxを実幅で表示確認した。すべての幅で全文、入れ子リスト、お問い合わせ先の横はみ出しとページ全体の横スクロールはなかった。
- 1920px、1600px、1599pxの変更前後スクリーンショットはそれぞれ画像ハッシュが一致し、既存のPC版本文幅、文字サイズ、ページ高が維持されていることを確認した。
- `ROADMAP.md` のステップ2-9を完了へ更新し、進行状況チェックのPrivacyを完了にした。次のフェーズ3には進んでいない。
- `ROADMAP.md` のフェーズ3・ステップ3-1を開始し、全9ページの表示・共通構造・既存JavaScript機能を総合確認した。CSSを再生成し、5個の生成物が再生成前後で一致した。
- FAQ、Contact、Archive、Single、Privacyの保存済み実幅レポートを再集計し、1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxの全45状態でページ全体の横スクロールと画面外要素がないことを確認した。
- TOP、About、Facilities、Priceは保存済みスクリーンショットと過去の実幅確認記録を目視・再確認した。全9ページの代表画像で大きな表示崩れ、画像比率の異常、フッターの横はみ出しは確認されなかった。
- FAQのクリック・Enter・Space、Contactフォーム、Archive絞り込み、Singleのリンクは保存済み操作レポートで正常であることを再確認した。全9ページの画像・CSS・JavaScript参照、ページ内リンク、`aria-controls`、スキップターゲットにも欠損はなかった。
- 総合確認で、共通ヘッダーの「ご利用の流れ」が存在しない `about_us.html#flow` を指す問題を確認し、`main.js` のリンク先を実在する `about_us.html#flow__about` へ修正した。
- Singleページでは対応する現在地リンクがなく `aria-current` が付かなかったため、お知らせ詳細をArchive配下として扱い、フッターの「お知らせ一覧」を現在地にするよう `main.js` を修正した。
- 修正後の `main.js` は構文検査と最小DOM実行テストを行い、ヘッダーリンク生成、Single・Privacyの現在地、メニュー開閉、Escapeでのフォーカス復帰が正常であることを確認した。HTML文章・既存クラス・SCSSは変更していない。
- ローカル確認用URLへ接続し、修正後の全9ページを1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxの全81状態で新規ライブ確認した。ページ全体の横スクロール、画像読み込み失敗、フッターのはみ出し、操作要素の文字切れは確認されなかった。
- Priceの430px・429pxでは620px幅の料金表が幅383pxの `.table-wrap` 内だけを横スクロールし、ページ全体の幅は415px・414pxに収まっていることを確認した。
- TOP、About、Facilities、Priceの1920pxと430pxを新規に目視確認し、Aboutのモバイル用フロー画像も対象位置で確認した。大きな表示崩れや画像比率の異常は確認されなかった。
- モバイルメニューの開閉、Escapeでのフォーカス復帰、メニュー項目の移動、修正した `about_us.html#flow__about` の着地点、スキップリンク、Singleの現在地、Archiveのカテゴリ・月別絞り込み、FAQのクリック開閉と `aria-expanded` を実画面で確認した。ブラウザーのエラーログは0件だった。
- `ROADMAP.md` のステップ3-1を完了へ更新した。この追加確認ではサイトのコードとCSSを変更しておらず、ステップ3-2には進んでいない。
- `ROADMAP.md` のフェーズ3・ステップ3-2を開始し、当日の作業履歴、ファイル更新時刻、現在のソース内容を照合した。サイトの実装変更は `scss/style.scss`、生成CSS5ファイル、`js/main.js` に限定されていた。
- 全9個のHTMLは作業開始後の更新が0件で、文章、既存クラス、仮データに変更がないことを確認した。
- レスポンシブSCSSは指定ブレークポイント内にまとまり、各ページ固有部分は `body[data-type="..."]` 配下に限定されていた。ステップ3-1のPC表示確認結果と合わせ、PC版に関係のない大規模変更がないことを確認した。
- `main.js` は「ご利用の流れ」のアンカー修正とSingleの現在地設定が作業履歴と一致し、構文検査も正常だった。
- 標準のCSSビルド実行は環境側の外部検証処理が再試行となったため終了し、同じ既存の `tools/build-css.ps1` を直接実行した。生成対象5ファイルは再生成前後ですべて同一ハッシュとなり、`package.json` とロックファイルにも作業中の更新はなかった。
- 確認用スクリーンショットとブラウザープロファイルはルート直下の専用ディレクトリにまとまり、サイト配信ディレクトリ内への確認用生成物の混入は0件だった。
- 作業開始前から存在する末尾空白4行を確認したが、今回の変更範囲外のため整形していない。未解決の編集マーカーは0件だった。
- `ROADMAP.md` のステップ3-2と進行状況チェックのフェーズ3を完了へ更新した。フェーズ4には進んでいない。
- `ROADMAP.md` のフェーズ4・ステップ4-1を開始し、レビュー資料の完了条件、対象9ページ、必要な画面幅、既存のPC全体画像を確認した。
- `RESPONSIVE_REVIEW.md` を新規作成し、対象ページ一覧、基本幅、ブレークポイント直後、PC全体参考画像、変更概要、判断してほしい箇所、保留事項を1つの資料へ整理した。
- 1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxについて、全9ページの初期表示スクリーンショットを作成した。合計81枚で、比較しやすいように各画像を指定幅×高さ900pxへ統一した。
- 81枚のファイル存在と寸法を機械的に確認し、欠損0件、寸法不一致0件だった。資料内の81件の画像リンクと9件のPC全体画像リンクが実在することも確認した。
- TOPの1600px、1024px、768px、430pxとAboutの767pxを目視し、ヘッダー、ヒーロー、メニュー切り替え、画像比率、見出しに大きな崩れがないことを確認した。
- 1024pxのメニュー切り替え、768px以下の1列化、430pxの文字・余白、Aboutのフロー、Priceの表内横スクロールなど、レビュワーに判断してほしい箇所を資料へ明記した。
- 表示を妨げる保留事項はない。Priceの768px以下の料金表内横スクロールは、内容を省略しないための意図した仕様として注意点へ記載した。
- このステップでは `RESPONSIVE_REVIEW.md`、`ROADMAP.md`、`HISTORY.md` と確認用スクリーンショットだけを変更し、HTML、SCSS、CSS、JavaScriptには変更を加えていない。
- `ROADMAP.md` のステップ4-1を完了へ更新した。フェーズ4全体は未完了のままとし、ステップ4-2には進んでいない。
