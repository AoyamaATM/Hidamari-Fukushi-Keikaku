# ひだまり福祉計画 制作ロードマップ

## 目的

このファイルは、静的HTMLで制作している「ひだまり福祉計画」を、以下の順番で完成させるためのCodex向け作業手順書です。

1. スマホ・タブレット向けレスポンシブ対応
2. レビュワー確認と修正
3. GitHub Pagesでのオンライン公開
4. WordPressテーマ化

Codexは、一度に全工程を進めず、**各フェーズ・各ステップを順番に実施**してください。

---

## 現在の状態

- PC幅では全9ページの表示確認済み
- HTMLのページ識別属性は `data-type` に統一済み
- `main.js` も `body.dataset.type` を参照するよう修正済み
- スキップリンクの遷移確認済み
- FAQはクリック、Enterキー、Spaceキーで開閉できることを確認済み
- FAQ開閉時の `aria-expanded` 切り替え確認済み
- 仮の電話番号、人名、お知らせ内容などは、ポートフォリオ掲載用サンプルサイトの仕様として維持する
- PC版のデザイン・文章・既存機能を維持したまま、今後の作業を進める

---

## 共通作業ルール

作業前に必ず以下を確認してください。

- `AGENTS.md`
- 最新の `HISTORY.md`
- 対象HTML
- `scss/style.scss`
- `css/style.css`
- `js/main.js`
- `package.json`
- `README.md`
- `tools/visual-check-pages.json`
- 表示確認用スクリプト

### 変更ルール

- ユーザーの依頼に関係する範囲だけ変更する
- PC版の見た目を壊さない
- HTMLの文章を変更しない
- 仮データを正式データへ置き換えない
- 既存クラス名を無断で変更しない
- 既存のSCSS構造、変数、mixin、命名規則を優先する
- 不要な `!important` を追加しない
- 関係のないリファクタリングを同時に行わない
- SCSSを変更した場合はCSSをビルドする
- 作業後は変更内容、確認結果、未確認事項を報告する
- 各ステップ完了後に `HISTORY.md` を更新する
- 一度に複数フェーズを進めない
- 完了したステップには見出し部分に（完了）、途中のステップには見出し部分に（進行中）と書いて、進捗がわかるようにする
---

# フェーズ0：作業開始前の準備

## ステップ0-1：リポジトリ状態を確認する（完了）

### 実施内容

```bash
git status --short --branch
git pull --ff-only
```

### 完了条件

- 作業ブランチとリモートの状態を確認できている
- 未コミットの変更がある場合、その内容を把握できている
- 意図しない差分を残したまま次へ進んでいない

---

## ステップ0-2：レスポンシブ対応用ブランチを作る（完了）

### 実施例

```bash
git switch -c feature/responsive
```

既にレスポンシブ用ブランチがある場合は、新しく作らず既存ブランチを使用してください。

### 完了条件

- レスポンシブ対応が `main` とは別のブランチで進められる

---

## ステップ0-3：現在のPC版を基準として保存する（完了）

### 実施内容

```bash
pnpm run build:css
pnpm run check:visual:build
git diff --check
```

### 確認対象

- `index.html`
- `about_us.html`
- `facilities.html`
- `price.html`
- `faq.html`
- `contact.html`
- `archive.html`
- `single.html`
- `privacy.html`

### 完了条件

- 現在のPC版スクリーンショットが生成されている
- 変更前の基準画像として比較できる
- CSSビルドが成功している
- 空白エラーがない

---

# フェーズ1：共通部分のレスポンシブ対応

このフェーズでは、ページ固有の細かなレイアウトには手を入れず、全ページ共通部分を先に整えます。

## ステップ1-1：既存メディアクエリを調査する（完了）

### 調査対象

- 既存の `@media`
- ブレークポイント
- SP用クラス
- タブレット用指定
- 既にレスポンシブ化されている部品
- 横スクロールを起こしそうな固定幅
- `min-width`
- `width`
- `max-width`
- 絶対配置
- 大きな左右余白

### 使用するブレークポイント

このプロジェクトでは、以下のブレークポイントを使用します。
- 1600px
- 1024px
- 768px
- 430px

既存のメディアクエリについて、上記以外のブレークポイントが使われている場合は、すぐに変更せず、以下を確認してください。
- どのページ・セレクタに使用されているか
- 上記のブレークポイントへ統合できるか
- 統合した場合にPC版や中間幅へ影響が出ないか
- 特定の部品だけに必要な値ではないか
- 既存指定との重複や上書き順がないか

最低限、以下の幅を調査・確認対象とします。
- 1920px
- 1600px
- 1599px
- 1024px
- 1023px
- 768px
- 767px
- 430px
- 429px

必要に応じて、中間幅も確認してください。
- 1280px
- 900px
- 600px

### 調査結果

- `style.scss`、生成済みの `css/style.css`・`scss/style.css`・`scss/style.min.css`、およびHTML内に `@media` はなく、既存のブレークポイント・SP用クラス・タブレット用指定もない。したがって、1600px、1024px、768px、430px以外に統合を検討する指定はない。
- `main.js` にはモバイルメニュー用の `nav-open` 切り替えがある一方、対応するSCSSはなく、`.nav-toggle` は全幅で `display: none`。ヘッダー対応では既存のJavaScriptを再利用し、指定4幅だけで表示・開閉のスタイルを追加する。
- 既存の再利用可能な縮小対策は、全画像の `max-width: 100%`、`.page-shell` の `overflow: clip`、`--content` / `--comp-content` の可変コンテナ、`minmax(0, …)` を使った一部グリッド。これらを維持する。
- 固定幅・最小幅・絶対配置の主な確認箇所は、`.page-hero` / `.subpage-hero` の `width: 100vw`、共通グリッド、`.grid-footer`、`.footer-nav`、`.flow-assets__cta`、各ページの2列グリッド、`.price-links` / `.price-link-card`、`.category-cards`、`.post-grid`。以降の共通・ページ別ステップで対象ごとに調整する。
- 現時点では全幅に同じ宣言が適用されるため、1920px、1600px/1599px、1024px/1023px、768px/767px、430px/429pxおよび中間幅の境界で切り替わる既存指定はない。以降は各 `max-width` を1600px、1024px、768px、430pxの順に追加し、各境界の直前ではひとつ上の指定、境界以下では当該指定が適用されることを確認する。

### 完了条件

- 既存のレスポンシブ指定を再利用できる箇所が整理されている
- 1600px、1024px、768px、430px以外のメディアクエリが一覧化されている
- 上記以外の値を残す理由、または統合する理由を説明できる
- 新規メディアクエリを追加する前に、既存指定との重複を確認できている
- ブレークポイントの直前・直後で適用される指定を把握できている

---

## ステップ1-2：共通コンテナを調整する（完了）

### 対象

- `html`
- `body`
- `.page-shell`
- `.content-width`
- `.section`
- 共通グリッド
- 共通画像

### 実施内容
- 1600px以下では、コンテンツ最大幅と左右余白のバランスを調整する
- 1024px以下では、固定幅や複数列レイアウトが画面幅を超えないようにする
- 768px以下では、タブレット縦向けに左右余白とグリッド構成を調整する
- 430px以下では、スマートフォン向けにコンテンツ幅・余白・画像サイズを調整する
- 画面幅を超える固定幅を整理する
- 左右余白を各ブレークポイントに合わせて段階的に縮小する
- 画像の縦横比を維持する
- 不要な横スクロールを防ぐ
- 1600pxより広いPC幅では、現在の最大幅とレイアウトを維持する
- ブレークポイントごとに同じ指定を重複させず、必要な差分だけを上書きする
- `width: 100vw`、固定px幅、絶対配置、負のmarginが横スクロールの原因になっていないか確認する

### 実施結果

- コンテナ変数を1600px、1024px、768px、430pxで段階化し、左右余白をそれぞれ32px/28px、28px/24px、24px/20px、16px/16px（通常ページ/共通コンポーネント用）へ縮小した。1920px以上の最大幅1600pxは維持している。
- コンテナ計算とヒーロー幅の `100vw` を `100%` へ置き換え、共通画像の縦横比保持、`.content-width` と共通グリッド子要素の最小幅0を追加した。スクロールバー幅やグリッド内コンテンツによる不要な横方向の拡張を抑える。
- 1024px、768px、430px以下では、共通セクションの上下余白を88px以下、64px、48pxへ段階的に縮小した。ページ固有のグリッド構成・ヘッダー・ボタン・フッターは後続ステップで調整する。
- 1920pxから429pxまでの境界直前・直後と中間幅で、全9ページの共通コンテナ幅が指定どおり変化することを確認した。430pxでは全9ページとも実コンテナ幅383px（表示領域415px）だった。
- 430px時に残るページ全体の横はみ出しは、ヘッダーナビゲーション、TOP固有ヒーロー、固定幅CTA、フッターナビゲーションに由来する。これらはステップ1-3、1-4、1-5の対象であり、共通コンテナの変更では先取りしない。フェーズ1-6で全体の横スクロールを再確認する。

### 完了条件
- 1920px以上で現在のPC版レイアウトが維持されている
- 1600px、1024px、768px、430pxでコンテンツが画面幅に収まる
- 1599px、1023px、767px、429pxで急なレイアウト崩れがない
- 430px以下でページ全体に横スクロールが発生しない
- PC幅のコンテナ最大幅と余白が意図せず変わっていない
- 画像が各幅で画面外にはみ出さない

---

## ステップ1-3：ヘッダーとナビゲーションを調整する（完了）

### 対象

* `.site-header`
* `.header-inner`
* `.brand`
* `.brand-logo`
* `.site-nav`
* `.nav-toggle`
* `.nav-toggle-lines`
* `.nav-open`
* `.skip-link`
* `.skip-target`

### 使用するブレークポイント

このプロジェクトでは、以下のブレークポイントを使用します。

* 1600px
* 1024px
* 768px
* 430px

新しいメディアクエリを追加する場合は、原則として上記の値を使用してください。

既存のメディアクエリに別の値が使われている場合は、すぐに書き換えず、対象セレクタと表示への影響を確認してください。今回のヘッダー対応に関係する範囲のみ、上記のブレークポイントへ整理してください。

### 実施内容

* 1600px、1024px、768px、430pxの各ブレークポイントでヘッダーを確認する
* 各ブレークポイントの直前・直後で、レイアウトが急に崩れないか確認する
* 1600px以下では、ロゴ、ナビゲーション、左右余白のバランスを確認する
* 1024px以下では、PC用ナビゲーションを維持できるか、モバイルメニューへ切り替える必要があるかを既存実装に基づいて判断する
* 768px以下では、ヘッダー内のロゴとメニューボタンが画面幅に収まるようにする
* 430px以下では、スマートフォン向けの最終的な幅・余白・文字サイズを調整する
* モバイルメニューの開閉を確認する
* `aria-expanded` の既存動作を維持する
* メニュー展開時に、ナビゲーションが画面外へはみ出さないようにする
* メニュー展開時に、ページ全体へ不要な横スクロールが発生しないようにする
* メニュー項目がタップしやすい間隔になるように調整する
* ロゴが小さくなりすぎないようにする
* ロゴ画像の縦横比を維持する
* メニューボタンとロゴが重ならないようにする
* ナビゲーションを開いた状態で、すべてのメニュー項目を確認できるようにする
* スキップリンクの表示・フォーカス・移動動作を壊さない
* `.skip-target` の位置を変更しない
* 固定ヘッダーとアンカー移動先の関係を確認する
* 現在地ナビゲーションの `aria-current="page"` を維持する
* PC版のヘッダー構造とデザインを基準とし、レスポンシブ対応に必要な範囲だけ変更する

### 確認幅

最低限、以下の幅で確認してください。

* 1920px：1600pxより広いPC表示
* 1600px：第1ブレークポイント
* 1599px：1600px以下の指定確認
* 1024px：第2ブレークポイント
* 1023px：1024px以下の指定確認
* 768px：第3ブレークポイント
* 767px：768px以下の指定確認
* 430px：第4ブレークポイント
* 429px：430px以下の指定確認

必要に応じて、中間幅でも確認してください。

* 1280px
* 900px
* 600px

### 操作確認

* Tabキーでスキップリンクへ移動できる
* スキップリンクを実行すると、想定している本文位置へ移動する
* Tabキーでモバイルメニューの開閉ボタンへ移動できる
* EnterキーまたはSpaceキーでモバイルメニューを開閉できる
* メニュー開閉時に `aria-expanded` が正常に切り替わる
* メニューを開いた後、すべてのナビゲーションリンクへTabキーで移動できる
* 現在のページに対応するリンクへ `aria-current="page"` が付与される

### 完了条件

* 1920px以上のPC幅で、既存ヘッダーの見た目が維持されている
* 1600px、1024px、768px、430pxの各ブレークポイントでヘッダーが崩れない
* 430px以下でナビゲーションを正常に開閉できる
* ロゴとメニューボタンが重ならない
* メニュー項目をすべて表示・操作できる
* メニュー展開時に横スクロールが発生しない
* `aria-expanded` が正常に切り替わる
* `aria-current="page"` が正常に付与される
* スキップリンクが想定している位置へ移動する
* キーボードだけで主要なヘッダー操作を行える
* PC版ヘッダーに意図しない変化がない
* 変更内容と各幅での確認結果が報告されている

### 実施結果

* 1024px以下でPCナビゲーションをメニューボタンへ切り替え、展開時はヘッダー直下に縦並びのナビゲーションを表示するようにした。各リンクは最小56pxを確保し、展開中もナビゲーション自身の横はみ出しがない。
* 768px以下ではヘッダーを100pxに調整し、430px以下ではFigmaのデザインカンプに合わせて、ヘッダー120px、ロゴ267px×80px、メニューボタン80pxを基準にした。ロゴの縦横比は維持している。
* 既存の`aria-expanded`、`aria-current="page"`、スキップリンクと`.skip-target`の`scroll-margin-top: 110px`を維持した。メニューはEscapeで閉じてボタンへフォーカスを戻し、PC幅への復帰時にも開閉状態をリセットする。
* 1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxで確認した。PC幅では既存の90pxヘッダーと横並びナビゲーションを維持し、1024px以下ではヘッダーの幅内にロゴ・ボタン・ナビゲーションが収まることを確認した。430pxでの実表示領域はスクロールバーを除く415pxのため、ロゴは253.5px×76pxに縮小表示されるが、通常の430px表示領域ではFigma基準の267px×80pxとなる。
* 430pxでメニューを展開し、全6リンクの表示、`aria-expanded`の切り替え、ナビゲーション単体の横スクロールなし、施設紹介ページでの`aria-current="page"`を確認した。ネイティブ`button`を用いるため、EnterとSpaceによる開閉にも対応する。


---

## ステップ1-4：共通見出し・ボタン・パンくずを調整する（完了）

### 対象

- `.section-heading`
- `.center-heading`
- `.button`
- `.button--cta`
- `.button-row`
- `.breadcrumb`
- `.breadcrumb--plain`
- `.asset-link`

### 実施内容
- 1600px以下では、PC版を基準に大きな見出しやボタンの余白バランスを確認する
- 1024px以下では、見出し・ボタン・パンくずが横幅に収まるよう調整する
- 768px以下では、タブレット縦向けに文字サイズと左右余白を調整する
- 430px以下では、スマートフォン向けに文字サイズ・行間・ボタン幅を調整する
- 長い見出しが画面幅に収まるようにする
- 文字サイズを各ブレークポイントで段階的に調整する
- ボタンの文字が切れないようにする
- ボタンが画面外にはみ出さないようにする
- ボタンを必要以上に小さくしない
- パンくずが長い場合に自然に折り返すようにする
- パンくず内のリンクや区切り文字が不自然に分離しないか確認する
- タップ対象同士の間隔を確保する
- 1600pxより広いPC版の文字サイズ・余白は維持する

### 完了条件
- 1600px、1024px、768px、430pxで見出しとボタンが画面幅に収まる
- 430px以下でボタンの文字が切れない
- パンくずが横にはみ出さない
- 1599px、1023px、767px、429pxで急な文字サイズ変化や崩れがない
- PC版の文字サイズと余白が維持されている

### 実施結果

* `.section-heading` に幅制限と長文折り返しを追加し、TOP・施設紹介・料金表で固定だった40pxの主要見出しを1024px以下から段階的に縮小した。プライバシーポリシーの`.section-heading__title`も同じ方針で調整し、430pxでは26.4px・2行表示に収まる。
* `.button` は長い文言を折り返せるようにし、768px以下でCTAの固定100px高さを最小84px、430px以下では最小72pxへ切り替えた。430pxでCTAとフォーム送信ボタンが画面幅内に収まり、文字の切れがないことを確認した。
* パンくずの左右余白を共通コンテナに合わせ、通常・簡易パンくずともに14pxまで段階的に縮小した。リンクを改行させず、最終項目だけを自然に折り返せるようにして、430pxで横にはみ出さないことを確認した。
* `.asset-link`には共通の最大幅・最小幅を追加した。料金表の画像リンクは親の固定600pxグリッドにより画面外へ出るため、ページ固有のステップ2-4でグリッドと合わせて調整する。
* 1600px、1599px、1024px、1023px、768px、767px、430px、429pxで確認した。各境界前後で共通見出し・CTA・パンくずの文字サイズと要素幅に急な変化がなく、1600px以上のPC表示は維持されている。

---

## ステップ1-5：フッターを調整する（完了）

### 対象

- `.footer`
- `.grid-footer`
- `.footer-nav`
- `.footer-address`
- `.copyright`

### 実施内容
- 1600px以下では、フッター内の最大幅と左右余白を確認する
- 1024px以下では、複数列を維持できるか確認し、必要に応じて列数や間隔を調整する
- 768px以下では、フッター内の情報を1列または少ない列数へ変更する
- 430px以下では、ロゴ、住所、ナビゲーション、コピーライトを縦方向に読みやすく配置する
- PCの複数列構成を、画面幅に応じて段階的に変更する
- リンク間隔を調整する
- 住所・電話番号の折り返しを確認する
- ロゴ、住所、ナビゲーション、コピーライトの順序を確認する
- フッターナビゲーションが画面外にはみ出さないようにする
- 1600pxより広いPC版の配置を維持する

### 完了条件
- 全9ページの1920px以上のPC幅で大きな崩れがない
- 共通部分が1600px、1024px、768px、430pxで正常に表示される
- 1599px、1023px、767px、429pxでも急な崩れがない
- 必要に応じて1280px、900px、600pxの中間幅も確認されている
- 各幅で横スクロールが発生していない
- ヘッダー、見出し、ボタン、パンくず、フッターが画面幅に収まる
- 既存のJavaScript機能が正常に動作する
- PC版へ意図しない変更がない - 未確認事項が報告されている

### 実施結果

* 1024px以下ではフッターを情報・ナビゲーションの2列に保ちつつ、縦書きナビゲーションを4列へ変更した。各列に十分な幅を確保し、リンクの横はみ出しを防いでいる。
* 768px以下ではロゴ・住所とナビゲーションを1列にし、ナビゲーションを横書き4列へ切り替えた。430px以下では2列・最小48pxのタップ領域にし、ロゴ、住所、ナビゲーション、コピーライトの順で縦に読める構成にした。
* 430pxではロゴ267px×80px、住所383px幅、ナビゲーション2列（各183.5px）、コピーライト383px幅に収まり、フッター単体の横スクロールがないことを確認した。住所と長いナビゲーション項目は必要に応じて自然に折り返せる。
* 1920px、1600px、1599px、1280px、1024px、1023px、900px、768px、767px、600px、430px、429pxで確認した。各境界前後で急な崩れがなく、1600pxより広いPC版は既存の2列・縦書き8列ナビゲーションを維持している。
* JavaScriptによるフッターナビゲーションの`aria-current="page"`設定が維持され、TOPページでは「ご利用の流れ」に現在地が設定されることを確認した。

---

## ステップ1-6：共通部分の確認

### 実行

```bash
pnpm run build:css
pnpm run check:visual:build
git diff --check
git status --short
```

SP確認用コマンドが用意されている場合は、以下も実行してください。

```bash
pnpm run check:visual:sp
```

### 完了条件

- 全9ページのPC幅で大きな崩れがない
- 共通部分が375px、390px、768px、1024pxで正常
- 横スクロールが発生していない
- 未確認事項が報告されている

---

# フェーズ2：ページ固有のレスポンシブ対応

ページごとに一度に進めず、以下のグループ単位で対応します。

---

## ステップ2-1：TOPページ

### 対象

- `index.html`
- TOPページに関係するSCSS

### 主な確認箇所

- メインビジュアル
- キャッチコピー
- ページ内リンク
- 選ばれる理由
- サービス概要
- ご利用開始までの流れ
- 料金の目安
- お知らせ
- FAQ
- ページ下部

### 実施内容
- 1600px以下では、メインビジュアル、コンテンツ最大幅、左右余白を確認する
- 1024px以下では、複数列レイアウトやページ内リンクの列数を調整する
- 768px以下では、画像と文章を必要に応じて縦並びに変更する
- 430px以下では、スマートフォン向けに文字サイズ、余白、画像幅を調整する
- `.grid-route` の列数を各ブレークポイントに合わせて変更する
- `.grid-reason` を画面幅に応じて縦並びにする
- `.grid-reason--reverse` の表示順を確認する
- サービス画像と説明文を画面幅に収める
- `.flow-assets--home` の画像を各幅で読みやすくする
- お知らせ一覧を画面幅に応じて縦積みにする
- FAQの長文と開閉アイコンを調整する
- CTAが画面外にはみ出さないようにする
- 1600pxより広いPC版の配置を維持する

### 完了条件
- TOPページが1600px、1024px、768px、430pxで最後まで読める
- 1599px、1023px、767px、429pxで急な崩れがない
- 430px以下で横スクロールがない
- FAQをクリック、Enter、Spaceで開閉できる
- PC版に大きな差分がない

---

## ステップ2-2：施設紹介ページ

### 対象

- `about_us.html`
- Aboutページに関係するSCSS

### 主な確認箇所

- `.profile-grid`
- `.service-showcase`
- `.about-day-grid`
- `.stack-gallery`
- `.grid-split`
- `.about-service-block`
- `.flow-assets--about`
- `.flow-assets__item`
- `.flow-assets__cta`

### 実施内容
- 1600px以下では、コンテンツ最大幅と画像サイズのバランスを確認する
- 1024px以下では、2列構成の幅と間隔を調整する
- 768px以下では、必要な2列構成を1列へ変更する
- 430px以下では、文章、リスト、画像、CTAをスマートフォン向けに調整する
- 写真の並び順を確認する
- サービス詳細の文章とリストを読みやすくする
- ご利用開始までの流れを各幅で読みやすくする
- CTAと画像の重なりが崩れないようにする
- 1600pxより広いPC版の配置を維持する

### 完了条件
- 1600px、1024px、768px、430pxで全セクションが画面幅に収まる
- 1599px、1023px、767px、429pxで急な崩れがない
- 430px以下で流れ画像とCTAが重ならない
- PC版に意図しない変化がない

---

## ステップ2-3：全施設紹介ページ

### 対象

- `facilities.html`
- Facilitiesページに関係するSCSS

### 主な確認箇所

- `.profile-grid`
- `.data-list`
- `.philosophy`
- `.grid-staff`

### 実施内容

- 1600px以下では、施設情報と画像の最大幅を確認する
- 1024px以下では、プロフィール・スタッフ紹介の列幅と間隔を調整する
- 768px以下では、画像と施設情報を縦並びに変更する
- 430px以下では、`dt`、`dd`、本文、画像の余白と文字サイズを調整する
- `dt` と `dd` の配置を読みやすくする
- 法人理念の長文を画面幅に収める
- スタッフ紹介を画面幅に応じて縦並びにする
- 1600pxより広いPC版の配置を維持する

### 完了条件
- 1600px、1024px、768px、430pxで施設情報とスタッフ情報が読める
- 1599px、1023px、767px、429pxで急な崩れがない
- `dt`、`dd`、画像が画面外にはみ出さない
- PC版に意図しない変化がない

---

## ステップ2-4：料金表ページ

### 対象

- `price.html`
- Priceページに関係するSCSS

### 主な確認箇所

- `.price-links`
- `.price-link-card`
- `.table-stack`
- `.table-wrap`
- `.table-price`
- `.price-caption-note`

### 実施内容
- 1600px以下では、料金表の最大幅とコンテンツ余白を確認する
- 1024px以下では、表の列幅、文字サイズ、セル余白を調整する
- 768px以下では、表を画面内に無理に縮めるか、横スクロールに切り替えるか判断する
- 430px以下では、テーブル部分だけを安全に横スクロールできるようにする
- アンカーリンク画像を各幅で画面内に収める
- 4列テーブルを無理に狭めすぎない
- 必要に応じて `.table-wrap` に横スクロールを設定する
- 表の最小幅、文字サイズ、セル余白を調整する
- captionと補足文の折り返しを確認する
- ページ全体に横スクロールが発生しないようにする
- 1600pxより広いPC版の列幅を維持する

### 完了条件
- 1600px、1024px、768px、430pxで料金表の内容を確認できる
- 1599px、1023px、767px、429pxで表が不自然に潰れない
- ページ全体では横スクロールが発生しない
- テーブル部分だけ必要に応じて横スクロールできる
- PC版テーブルの列幅や文字サイズが変わっていない

---

## ステップ2-5：FAQページ

### 対象

- `faq.html`
- FAQページに関係するSCSS

### 主な確認箇所

- `.category-cards`
- `.faq-category-card`
- `.faq-category-card__notes`
- `.faq-stack`
- `.faq-question`
- `.faq-answer`
- `.faq-question-text`

### 実施内容

- 1600px以下では、カテゴリカードとFAQ本文の最大幅を確認する
- 1024px以下では、カテゴリカードの列数と間隔を調整する
- 768px以下では、カテゴリカードを少ない列数または縦並びへ変更する
- 430px以下では、質問文、回答文、開閉アイコン、`Q.`、`A.` の位置を調整する
- 補足リストを読みやすくする
- 長い質問文を自然に折り返す
- 開閉アイコンが文字と重ならないようにする
- 回答展開時の余白を確認する
- 1600pxより広いPC版の配置を維持する

### 完了条件
- 1600px、1024px、768px、430pxで全質問・回答が読める
- 1599px、1023px、767px、429pxで急な崩れがない
- FAQをクリック、Enter、Spaceで開閉できる
- `aria-expanded` が正常に切り替わる
- PC版に意図しない変化がない

---

## ステップ2-6：お問い合わせページ

### 対象

- `contact.html`
- Contactページ全体のレスポンシブ表示

### 実施内容

- 1600px以下では、ページ全体の最大幅と左右余白を確認する
- 1024px以下では、電話案内、説明文、ボタンの幅と間隔を調整する
- 768px以下では、各ブロックを画面幅に合わせて縦方向に整理する
- 430px以下では、文章、電話番号、ボタンが画面幅に収まるようにする
- 既存内容を変更しない
- WordPress化後に扱う予定の機能へは、この段階では踏み込まない
- 1600pxより広いPC版の配置を維持する

### 完了条件
- 1600px、1024px、768px、430pxでページ全体が画面幅に収まる
- 1599px、1023px、767px、429pxで急な崩れがない
- 430px以下で横スクロールが発生しない
- PC版に意図しない変化がない

---

## ステップ2-7：お知らせ一覧ページ

### 対象

- `archive.html`
- Archiveページに関係するSCSS

### 主な確認箇所

- `.archive-shell`
- `.filter-bar`
- `.archive-select-field`
- `.list-archive`
- `.archive-item`
- `.pagination`

### 実施内容

- 1600px以下では、一覧全体の最大幅と余白を確認する
- 1024px以下では、絞り込み欄と一覧項目の列幅を調整する
- 768px以下では、絞り込み項目や一覧項目を必要に応じて縦並びにする
- 430px以下では、日付、タグ、タイトルを読みやすい順に縦方向へ配置する
- タイトルの折り返しを確認する
- ページネーションを画面幅に収める
- 絞り込み機能を壊さない
- 1600pxより広いPC版の配置を維持する

### 完了条件
- 1600px、1024px、768px、430pxで一覧を読める
- 1599px、1023px、767px、429pxで急な崩れがない
- カテゴリ・月別絞り込みが動作する
- ページネーションが画面幅に収まる
- PC版に意図しない変化がない

---

## ステップ2-8：お知らせ詳細ページ

### 対象

- `single.html`
- Singleページに関係するSCSS

### 主な確認箇所

- `.post-grid`
- `.post-article`
- `.post-meta`
- `.post-body`
- `.sidebar`
- `.sidebar-banner`
- `.list-compact`

### 実施内容

- 1600px以下では、本文とサイドバーの最大幅・間隔を確認する
- 1024px以下では、本文とサイドバーの列幅を調整する
- 768px以下では、本文・サイドバーの2列構成を1列へ変更する
- 430px以下では、見出し、メタ情報、本文、バナー、最新投稿を読みやすく調整する
- 本文を先、サイドバーを後に配置する
- サイドバーバナーを画面幅に収める
- 最新投稿一覧を読みやすくする
- 1600pxより広いPC版の2列構成を維持する

### 完了条件
- 1600px、1024px、768px、430pxで本文からサイドバーまで読める
- 1599px、1023px、767px、429pxで急な崩れがない
- 430px以下で横スクロールがない
- PC版の2列構成が維持されている

---

## ステップ2-9：プライバシーポリシーページ

### 対象

- `privacy.html`
- Privacyページに関係するSCSS

### 主な確認箇所

- `.policy`
- `.section-heading__title`
- 入れ子の `ul`
- 長文
- お問い合わせ先表記

### 実施内容

- 1600px以下では、本文最大幅と左右余白を確認する
- 1024px以下では、長文・見出し・リストの幅と行間を調整する
- 768px以下では、本文文字サイズと入れ子リストの余白を調整する
- 430px以下では、長いタイトル、住所、電話番号、入れ子リストを画面幅に収める
- 本文と箇条書きの文字サイズ・行間を調整する
- 入れ子リストの左余白を画面幅に合わせて調整する
- 1600pxより広いPC版の本文幅を維持する

### 完了条件
- 1600px、1024px、768px、430pxで全文を読める
- 1599px、1023px、767px、429pxで急な崩れがない
- リストが画面外にはみ出さない
- PC版に意図しない変化がない

---

# フェーズ3：レスポンシブ対応の総合確認

## ステップ3-1：全9ページの表示確認

### 確認幅

最低限、以下の幅で全9ページを確認してください。
- 1920px：1600pxより広いPC版の確認
- 1600px：第1ブレークポイント
- 1599px：1600px以下の指定確認
- 1024px：第2ブレークポイント
- 1023px：1024px以下の指定確認
- 768px：第3ブレークポイント
- 767px：768px以下の指定確認
- 430px：第4ブレークポイント
- 429px：430px以下の指定確認

必要に応じて、以下の中間幅も確認してください。
- 1280px
- 900px
- 600px

### 共通チェック

- 横スクロールが発生していない
- テキストが画面外にはみ出していない
- 画像の縦横比が崩れていない
- ボタンの文字が切れていない
- タップ対象同士が過度に接近していない
- ヘッダーを開閉できる
- メニュー項目を操作できる
- スキップリンクが動作する
- アンカー移動先が不自然でない
- `aria-current="page"` が正常
- FAQがクリック、Enter、Spaceで開閉できる
- FAQの `aria-expanded` が正常
- Archiveの絞り込みが動作する
- フッターが画面幅に収まる

### 実行

```bash
pnpm run build:css
pnpm run check:visual:build
pnpm run check:visual:sp
git diff --check
git status --short
```

存在しないコマンドは勝手に作り直さず、既存の `package.json` とツールを確認したうえで対応してください。

### 完了条件

- 全9ページの1920px、1600px、1024px、768px、430pxで確認が完了している
- 1599px、1023px、767px、429pxでブレークポイント直後の表示を確認している
- 必要に応じて1280px、900px、600pxでも確認している
- 大きな表示崩れがない
- ページ全体に不要な横スクロールがない
- 既存JavaScript機能が動作する
- PC版に意図しない変化がない
- 確認結果が `HISTORY.md` に記録されている

---

## ステップ3-2：差分を整理する

### 実行

```bash
git diff --check
git status --short
git diff
```

### 完了条件

- 意図しないHTML文章変更がない
- 不要なクラス変更がない
- PC版に関係のない大規模変更がない
- 生成CSSが同期されている
- 確認用生成物が誤ってGit管理対象に入っていない

---

# フェーズ4：レビュワー確認

## ステップ4-1：レビュー用資料を用意する

### 用意するもの

- 1920px版スクリーンショット
- 1600px版スクリーンショット
- 1024px版スクリーンショット
- 768px版スクリーンショット
- 430px版スクリーンショット
- 必要に応じて、各ブレークポイント直後のスクリーンショット
  - 1599px
  - 1023px
  - 767px
  - 429px
- 対応したページ一覧
- 判断してほしい箇所
- 保留している箇所
- Gitブランチまたは確認用URL

### 完了条件

- レビュワーが1600px、1024px、768px、430pxの各状態を確認できる
- どのブレークポイントで何を変更したか説明できる
- PC版と各レスポンシブ版を比較できる
- 確認用URLまたはスクリーンショットが揃っている

---

## ステップ4-2：レビュー指摘を整理する

レビュー指摘は以下の形式で記録してください。

```md
## レスポンシブレビュー修正

### 共通
- ヘッダー：
- ナビゲーション：
- 見出し：
- セクション余白：
- フッター：

### TOP
-

### About
-

### Facilities
-

### Price
-

### FAQ
-

### Contact
-

### Archive
-

### Single
-

### Privacy
-
```

### 完了条件

- 指摘内容がページ、画面幅、対象箇所ごとに整理されている
- 曖昧な指示は、実装前に確認事項として分けられている

---

# フェーズ5：レビュー修正

## ステップ5-1：修正用ブランチを用意する

必要に応じて以下を実行します。

```bash
git switch -c fix/responsive-review
```

### 完了条件

- 初回レスポンシブ対応とレビュー修正を区別できる

---

## ステップ5-2：指摘を小分けに修正する

### 作業ルール

- 共通修正から先に行う
- ページ固有修正はその後に行う
- 対象幅を明確にする
- 指摘されていない部分を変更しない
- PC版を壊さない
- 1つの指摘ごとに確認する
- 修正後は全9ページの影響を確認する

### 完了条件

- 各レビュー指摘に対応済み・保留・対象外のいずれかを記録できている
- 再レビュー用スクリーンショットを用意できている

---

## ステップ5-3：再レビュー

### 完了条件

- レビュワーからレスポンシブ対応完了の確認を得ている
- 残る指摘がある場合は、フェーズ5を繰り返す

---

# フェーズ6：静的サイト完成版の確定

## ステップ6-1：レスポンシブ対応を統合する

ブランチ構成を確認したうえで統合してください。

### 実施例

```bash
git switch main
git pull --ff-only
git merge feature/responsive
```

レビュー修正用ブランチがある場合は、その内容を先にレスポンシブブランチへ統合してから `main` へ反映してください。

### 完了条件

- レビュー済みレスポンシブ対応が `main` に入っている
- コンフリクトが解消されている
- 全9ページの最終確認が成功している

---

## ステップ6-2：静的サイト完成版のタグを付ける

### 実施例

```bash
git tag static-v1.0
git push origin main
git push origin static-v1.0
```

### 完了条件

- WordPress化前の静的サイト完成版へ戻れる
- GitHub上にタグが存在する

---

# フェーズ7：GitHub Pages公開

## ステップ7-1：公開構成を確認する

### 確認項目

- `index.html` の場所
- HTML、CSS、JavaScript、画像の相対パス
- 大文字・小文字の不一致
- ローカル専用パスの有無
- 公開対象外ファイル
- Git管理されていない必要画像
- 生成CSSの有無

### 完了条件

- GitHub Pagesで読み込める静的構成になっている
- Windows上だけで成立するパスがない
- 必要ファイルがGitHubへpushされている

---

## ステップ7-2：GitHub Pagesを設定する

GitHubのリポジトリ画面で以下を設定します。

1. `Settings`
2. `Pages`
3. `Build and deployment`
4. `Source` を `Deploy from a branch`
5. 対象ブランチを選択
6. 公開フォルダを選択
7. `Save`

### 公開元の候補

- `main` ブランチの `/ (root)`
- `main` ブランチの `/docs`

現在のリポジトリ構造を確認し、`index.html` が公開元の先頭に来る構成を選んでください。

### 完了条件

- GitHub Pagesの公開URLが発行されている
- TOPページをオンラインで開ける

---

## ステップ7-3：公開URLで全ページを確認する

### 確認項目

- TOPページが表示される
- CSSが読み込まれる
- 画像が表示される
- JavaScriptが読み込まれる
- ヘッダーとフッターが表示される
- 全ページへ移動できる
- FAQが開閉する
- Archiveの絞り込みが動作する
- スキップリンクが動作する
- `aria-current="page"` が正常
- 直接URLを開いても各ページが表示される
- PCとスマートフォン実機で表示できる
- 404やリンク切れがない

### 完了条件

- GitHub Pages上で全9ページを閲覧できる
- ローカル版と大きな差がない
- 公開URLをレビュワーへ共有できる

---

# フェーズ8：WordPress化の準備

GitHub Pages公開が完了してから、このフェーズへ進みます。

## ステップ8-1：WordPress化用ブランチを作る

### 実施例

```bash
git switch -c feature/wordpress
```

### 完了条件

- 静的サイト完成版を維持したままWordPress化を開始できる

---

## ステップ8-2：静的HTMLとWordPressテンプレートの対応表を作る

| 静的ファイル | WordPress側の候補 |
|---|---|
| `index.html` | `front-page.php` |
| `about_us.html` | 固定ページテンプレート |
| `facilities.html` | 固定ページテンプレート |
| `price.html` | 固定ページテンプレート |
| `faq.html` | 固定ページテンプレート |
| `contact.html` | 固定ページテンプレート |
| `archive.html` | `archive.php` または `home.php` |
| `single.html` | `single.php` |
| `privacy.html` | 固定ページテンプレート |
| JS生成ヘッダー | `header.php` |
| JS生成フッター | `footer.php` |

### 完了条件

- 各HTMLをどのテンプレートへ移すか決まっている
- 共通部分とページ固有部分を分けられている

---

# フェーズ9：WordPressテーマの土台作成

## ステップ9-1：テーマの基本ファイルを作る

### 構成例

```text
theme-name/
├─ style.css
├─ functions.php
├─ header.php
├─ footer.php
├─ index.php
├─ front-page.php
├─ archive.php
├─ single.php
├─ page.php
├─ assets/
│  ├─ css/
│  ├─ js/
│  └─ img/
```

### 完了条件

- WordPress管理画面でテーマとして認識される
- テーマを有効化できる
- PHPエラーが出ない

---

## ステップ9-2：CSS・JavaScript・画像をテーマへ移す

### 実施内容

- CSSを `functions.php` から読み込む
- JavaScriptを `functions.php` から読み込む
- 画像パスをWordPress用へ変更する
- 相対リンクをWordPress用関数へ置き換える
- SCSSの編集元と生成先を明確にする

### 完了条件

- TOPページでCSS、JavaScript、画像が読み込まれる
- 静的版と同じデザインが表示される

---

## ステップ9-3：ヘッダーとフッターをPHP化する

### 実施内容

- JSで生成していたヘッダーを `header.php` へ移す
- JSで生成していたフッターを `footer.php` へ移す
- `get_header()` を使用する
- `get_footer()` を使用する
- WordPress側で現在地表示を実装する
- スキップリンクを維持する

### 完了条件

- 全テンプレートで共通ヘッダー・フッターが表示される
- JSによる重複生成がない
- ナビゲーションが動作する

---

# フェーズ10：WordPressページ移行

## ステップ10-1：TOPページを移行する

### 対象

- `index.html`
- `front-page.php`

### 完了条件

- TOPページがWordPress上で静的版と同じように表示される
- FAQなど既存のJavaScript機能が動作する
- PC・SPで表示確認できる

---

## ステップ10-2：固定ページを1ページずつ移行する

### 推奨順

1. About
2. Facilities
3. Price
4. FAQ
5. Privacy
6. Contact

### 作業ルール

- 1ページ移行するごとに表示確認する
- 全ページを一括変換しない
- 静的版のクラス構造を可能な範囲で維持する
- ページごとのスラッグを記録する

### 完了条件

- 各固定ページをWordPress上で直接開ける
- 静的版と大きな見た目の差がない
- PC・SPで表示確認できる

---

## ステップ10-3：お知らせ一覧と詳細を移行する

### 対象

- `archive.html`
- `single.html`
- `archive.php`
- `single.php`

### 実施内容

- タイトルをWordPressから取得する
- 投稿日をWordPressから取得する
- カテゴリをWordPressから取得する
- 本文をWordPressから取得する
- 最新投稿一覧を動的にする
- ページネーションをWordPress化する
- ダミー投稿を管理画面から登録する

### 完了条件

- 投稿一覧が表示される
- 投稿詳細が表示される
- カテゴリ、日付、本文が動的に出力される
- ページネーションが動作する

---

# フェーズ11：WordPress版の総合確認

## ステップ11-1：全ページ確認

### 確認幅

最低限、以下の幅でWordPress版の全ページを確認してください。
- 1920px
- 1600px
- 1599px
- 1024px
- 1023px
- 768px
- 767px
- 430px
- 429px

必要に応じて、以下の中間幅も確認してください。
- 1280px
- 900px
- 600px

### 確認項目

- ヘッダー
- フッター
- ナビゲーション
- 現在地表示
- パンくず
- スキップリンク
- TOPページ
- 固定ページ
- 投稿一覧
- 投稿詳細
- FAQ
- 画像パス
- CSS
- JavaScript
- リンク切れ
- PHPエラー
- 横スクロール
- キーボード操作

### 完了条件

- WordPress版の全ページが1600px、1024px、768px、430pxで表示できる
- 1599px、1023px、767px、429pxで急な表示崩れがない
- 1920px以上で静的版のPCデザインが維持されている
- 静的版と大きなデザイン差がない
- ページ全体に不要な横スクロールがない
- WordPress特有のリンク・投稿機能が動作する

---

# フェーズ12：最終整理

## ステップ12-1：ドキュメントを更新する

### 対象

- `README.md`
- `HISTORY.md`
- `AGENTS.md`
- 必要に応じてWordPress移行メモ

### 記録内容

- GitHub Pages公開URL
- 静的版タグ
- WordPressテーマ名
- CSSビルド方法
- 表示確認方法
- 使用ブランチ
- 未完了事項
- 今後の保守方法

### 完了条件

- 次に作業する人が手順を追える
- 静的版とWordPress版の区別が明確
- 未完了事項が明記されている

---

# Codexへの実行指示テンプレート

各ステップを依頼するときは、以下の形式を使用してください。

```md
`AGENTS.md`、最新の`HISTORY.md`、`ROADMAP.md`を確認してください。

今回は「フェーズX・ステップX-X」のみ実施してください。
次のフェーズやステップには進まないでください。

## 対象
- 対象ファイル
- 対象ページ
- 対象クラス
- 対象画面幅

## 要件
- PC版を維持する
- HTMLの文章を変更しない
- 仮データを変更しない
- 既存クラス名を無断で変更しない
- 既存のSCSS構造を優先する
- 必要最小限の変更に絞る
- SCSS変更後はCSSをビルドする
- 指定幅で表示確認する
- `git diff --check`を実行する

## 完了時の報告
- 変更したファイル
- 変更内容
- 実行した確認コマンド
- 確認できたこと
- 確認できなかったこと
- 残る注意点

完了後は作業を止め、次の指示を待ってください。
```

---

# 進行状況チェック

## レスポンシブ対応

- [x] フェーズ0：準備
- [ ] フェーズ1：共通部分
- [ ] フェーズ2-1：TOP
- [ ] フェーズ2-2：About
- [ ] フェーズ2-3：Facilities
- [ ] フェーズ2-4：Price
- [ ] フェーズ2-5：FAQ
- [ ] フェーズ2-6：Contact
- [ ] フェーズ2-7：Archive
- [ ] フェーズ2-8：Single
- [ ] フェーズ2-9：Privacy
- [ ] フェーズ3：総合確認
- [ ] フェーズ4：レビュワー確認
- [ ] フェーズ5：レビュー修正

## GitHub Pages

- [ ] フェーズ6：静的サイト完成版確定
- [ ] フェーズ7-1：公開構成確認
- [ ] フェーズ7-2：Pages設定
- [ ] フェーズ7-3：オンライン確認

## WordPress

- [ ] フェーズ8：WordPress化準備
- [ ] フェーズ9：テーマ土台
- [ ] フェーズ10-1：TOP移行
- [ ] フェーズ10-2：固定ページ移行
- [ ] フェーズ10-3：投稿機能移行
- [ ] フェーズ11：総合確認
- [ ] フェーズ12：最終整理
