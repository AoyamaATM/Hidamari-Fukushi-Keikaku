# 作業履歴

このファイルは、日ごとの作業状況を時次進捗形式で記録するためのものです。

## 2026-07-15

### 11時進捗

- 作業開始時に `AGENTS.md` の指示と `HISTORY.md` を確認し、前回分が 2026-07-14 の履歴であることを確認した。
- 日付が今日 2026-07-15 と異なるため、既存の `HISTORY.md` を `History-archive/HISTORY-260714.md` にアーカイブした。
- 今日分の新しい `HISTORY.md` を作成し、2026-07-15 の見出しから記録を開始した。
- `git status --short --branch` で `main...origin/main`、作業ツリーに差分がないことを確認した。
- `git pull --ff-only` でリモートは最新（Already up to date）であることを確認した。
- 現時点ではサイト本体、SCSS、CSS、JavaScript、HTMLには変更を加えていない。
- `ROADMAP.md` のフェーズ0・ステップ0-2を確認し、レスポンシブ対応用ブランチの作成手順と完了条件を確認した。
- ローカル・リモートともに既存の `feature/responsive` ブランチがないことを確認した。
- `git switch -c feature/responsive` で、`main` とは別のレスポンシブ対応用ブランチを作成して切り替えた。
- この時点ではステップ0-2のみ実施し、ステップ0-3以降には進んでいない。
- `ROADMAP.md` のフェーズ0・ステップ0-3を確認し、PC版基準スクリーンショット生成の対象9ページと完了条件を確認した。
- `README.md`、`package.json`、`tools/visual-check-pages.json`、表示確認用スクリプト、対象HTML、SCSS/CSS/JavaScriptの状態を確認した。
- `pnpm run build:css` を実行し、CSSビルドが成功することを確認した。
- 初回の `pnpm run check:visual:build` では `hidamari-fukushi-keikaku/scss/style.css` の一時的な読み込みエラーが出たため、`pnpm run build:css` を再実行してから再試行した。
- 再実行した `pnpm run check:visual:build` で、`visual-check/` にPC幅の全9ページ分スクリーンショットが生成されることを確認した。
- `git diff --check` で空白エラーがないことを確認した。
- 生成された `visual-check/` と `.chrome-check/` はローカル確認用のGit管理対象外で、サイト本体、SCSS、CSS、JavaScript、HTMLの差分は残っていないことを確認した。
- フェーズ0完了後の影響確認として、`feature/responsive` と `main` の差分が `HISTORY.md` と `ROADMAP.md` のみであることを確認した。
- `hidamari-fukushi-keikaku/`、`tools/`、`package.json`、`README.md`、`.browserslistrc` には `main` との差分がないことを確認した。
- `pnpm run build:css` を再実行し、ビルド後も作業ツリーに差分が出ないことを確認した。
- 追加のPC/SP表示確認は、Chrome/EdgeヘッドレスのGPUプロセス異常により再実行できなかった。ステップ0-3で生成済みのPC幅9ページスクリーンショットは `visual-check/` に残っている。
- 検証中に作成した一時プロファイルと空の検証ディレクトリを削除し、作業ツリーがクリーンであることを確認した。
- Chromeプラグイン経由の表示確認を試し、`file://` 直開きはブラウザ利用ポリシーで不可だったが、`python -m http.server 8765 --bind 127.0.0.1` でローカル配信したURLはChromeプラグインから閲覧できることを確認した。
- Chromeプラグインで全9ページを巡回し、タイトル、`body[data-type]`、CSS読み込み、画像読み込み、横スクロール有無、コンソールエラーを確認した。全ページでコンソールエラー、画像読み込み失敗、横スクロールは検出されなかった。
- Chromeプラグインのビューポート変更APIは呼び出せたが、実測の `window.innerWidth` には反映されなかったため、指定幅の厳密確認は従来のスクリーンショットスクリプト復旧後に行う必要がある。
- 確認後、Chromeプラグインのタブを閉じ、検証用ローカルHTTPサーバーを停止した。

### 12時進捗

- `ROADMAP.md` のステップ1-1に従い、対象9ページのHTML、`scss/style.scss`、生成CSS、`js/main.js`、`package.json`、`README.md`、表示確認用設定・スクリプトを調査した。
- `style.scss`、`css/style.css`、`scss/style.css`、`scss/style.min.css`、HTML内スタイルのすべてで `@media` が0件であることを確認した。既存のブレークポイント、SP用クラス、タブレット用指定はない。
- `main.js` の `nav-open` 切り替えは既存だが、対応SCSSはなく、`.nav-toggle` は全幅で非表示であることを確認した。ステップ1-3ではJavaScriptを維持して表示用のスタイルを追加する。
- 画像の `max-width: 100%`、可変コンテナ、`minmax(0, …)` は再利用可能な既存指定として維持する。`100vw`、固定・最小幅の複数列グリッド、絶対配置、固定幅のリンクカード類は以降の対象ステップで調整する。
- 既存のメディアクエリがないため、統合・削除すべき非標準ブレークポイントや境界直前・直後の既存上書きはない。以降は1600px、1024px、768px、430pxのみを追加し、各境界の前後で確認する方針を `ROADMAP.md` に記録した。
- ステップ1-1は調査のみでSCSS/CSS・HTML・JavaScriptに変更はなく、`git diff --check` で空白エラーがないことを確認した。残作業はステップ1-2の共通コンテナ調整。
- ステップ1-2として `scss/style.scss` の共通コンテナを調整し、生成CSSを `pnpm run build:css` で同期した。`100vw` に依存したコンテナ・ヒーロー幅を `100%` に置き換え、画像の縦横比と共通グリッド内の縮小を保護した。
- 1600px、1024px、768px、430pxの4段階で、通常ページと共通コンポーネント用コンテナの左右余白、および共通セクションの上下余白を段階的に縮小する指定を追加した。1920px以上の最大幅1600pxは維持している。
- `pnpm run check:visual:build` でPC幅1600pxの全9ページのスクリーンショットを生成し、TOP・施設紹介・料金表を目視確認した。共通コンテナ変更によるPC版の意図しない崩れは確認されなかった。
- SP用スクリーンショットスクリプトは既存ChromeプロファイルのGPU・キャッシュ競合で失敗したため、別プロファイルの430px撮影とローカルブラウザで代替確認した。1920px、1600px/1599px、1280px、1024px/1023px、900px、768px/767px、600px、430px/429pxでコンテナ変数と実幅を確認した。
- 430pxでは全9ページの `.content-width` が表示領域415px内の383pxになり、共通コンテナは収まった。ページ全体の横はみ出しはヘッダーナビ、TOP固有ヒーロー、固定幅CTA、フッターナビに由来するため、ステップ1-3〜1-5で解消し、ステップ1-6で全体を再確認する。
- `git diff --check` で空白エラーがないことを確認した。残作業はステップ1-3のヘッダーとナビゲーション調整。
- ステップ1-3として、1024px以下でPCナビゲーションをメニューボタンへ切り替え、展開時にはヘッダー直下の縦並びメニューを表示するSCSSを追加した。リンクは最小56pxを確保し、ナビゲーション単体の横はみ出しを防いでいる。
- 430px以下ではFigmaカンプを基準に、ヘッダー120px、ロゴ267px×80px、メニューボタン80pxへ調整した。768px以下の100pxヘッダー、1024px以下の96pxヘッダーも追加し、PC版の90pxヘッダーは維持した。
- `main.js` では既存の`aria-expanded`と`nav-open`を維持しつつ、Escapeでのメニュークローズ・フォーカス復帰と、PC幅へ戻った際の開閉状態リセットを追加した。`aria-current="page"`、スキップターゲット、`scroll-margin-top: 110px`は変更していない。
- ローカルブラウザで1920px、1600px、1599px、1024px、1023px、768px、767px、430px、429pxを確認し、各幅でヘッダー自体の横はみ出しがないことを確認した。430pxでは展開状態の全6リンク、`aria-expanded`の切り替え、ナビゲーション単体に横スクロールがないこと、施設紹介ページの`aria-current="page"`を確認した。
- ステップ1-4として、共通見出しに幅制限と長文折り返しを追加し、TOP・施設紹介・料金表の固定40px見出しを1024px以下から段階的に縮小した。プライバシーポリシーの`.section-heading__title`も対象に含め、430pxでは26.4px・2行で収まることを確認した。
- `.button`の長文折り返しを共通化し、CTAは768px以下で最小84px、430px以下で最小72pxの可変高さにした。430pxで施設紹介のCTAとお問い合わせフォームの送信ボタンが画面幅内に収まり、文字が切れないことを確認した。
- パンくずの左右余白を共通コンテナに合わせ、通常・簡易パンくずともに430pxで横にはみ出さないことを確認した。リンクは改行せず、最終項目だけを自然に折り返せる指定にした。
- `.asset-link`へ共通の最大幅・最小幅を追加した。料金表の画像リンクは親グリッドが固定600pxのため画面外に出る状態が残っており、ステップ2-4で親グリッドと合わせて調整する。TOPのヒーロー由来の横はみ出しもステップ2-1の対象として残る。
- ローカルブラウザで1600px、1599px、1024px、1023px、768px、767px、430px、429pxを確認し、各境界前後で共通見出し・CTA・パンくずの急な変化がないことを確認した。`pnpm run check:visual:build`でPC幅の全9ページを再生成し、TOPページのPC表示に意図しない崩れがないことを確認した。`git diff --check`でも空白エラーはなかった。
