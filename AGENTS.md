# AGENTS.md

## 目的
このファイルは、このリポジトリで作業するコーディングエージェント向けの指針をまとめたものです。

## 作業方針
- 変更を始める前に、既存のコードやドキュメントを確認する。
- ユーザーの依頼に関係する範囲に変更を絞る。
- 既存の設計、命名、ライブラリ、実装パターンを優先する。
- 関係のないリファクタリングや整形だけの変更は避ける。
- 明示的な指示がない限り、ユーザーの変更を上書きしたり元に戻したりしない。

## 作業履歴
- 作業開始時は、ルート直下の `HISTORY.md` を確認し、前回までの作業状況を把握する。
- `HISTORY.md` の日付が現在日付と異なる場合は、その日の初回作業開始時に既存の `HISTORY.md` をアーカイブする。
- アーカイブ先はルート直下の `History-archive` ディレクトリとする。存在しない場合のみ作成し、次回以降は同じディレクトリを使用する。
- アーカイブファイル名は `HISTORY-yymmdd.md` とし、例として 2026-06-03 の履歴は `HISTORY-260603.md` とする。
- アーカイブ後は、新しい当日分の `HISTORY.md` を作成し、現在日付の見出しから記録を始める。
- 作業内容は時次進捗形式で記録する。`10:00〜11:00` の作業は `11時進捗`、`11:00〜12:00` の作業は `12時進捗`、`12:00〜14:00` の作業は `14時進捗`、`14:00〜14:50` の作業は `15時進捗`、`14:50〜15:45` の作業は `16時進捗` のように、作業枠の終わり側の時刻でまとめる。
- 進捗には、変更したページ・ファイル、主な調整内容、確認したこと、残り作業や注意点を簡潔に書く。

## 実装
- 必要な課題を適切に解決できる、できるだけ小さな変更を行う。
- 振る舞いを変更した場合は、必要に応じてテストを追加または更新する。
- 名前は分かりやすくし、コメントは必要な箇所に簡潔に書く。
- 一時的な近道より、保守しやすい実装を優先する。

## 確認
- 可能な範囲で、変更内容に合ったテストや確認コマンドを実行する。
- 確認できたことと、確認できなかったことを報告する。
- 前提や注意点、残るリスクがあれば明確に伝える。

## SCSS/CSS運用
- CSSを変更する場合は、原則として `hidamari-fukushi-keikaku/scss/style.scss` を編集する。
- HTMLが読み込む表示用CSSは `hidamari-fukushi-keikaku/css/style.css` とする。
- `hidamari-fukushi-keikaku/css/style.css`、`hidamari-fukushi-keikaku/scss/style.css`、`hidamari-fukushi-keikaku/scss/style.min.css` は生成物として扱う。
- SCSS変更後は `pnpm run build:css` を実行して生成CSSを同期する。
- CSS作業中の監視には `pnpm run watch:css` を使用できる。
- 緊急時を除き、生成CSSだけを直接編集しない。直接編集した場合も、最終的にはSCSSへ反映してビルド結果と同期する。

## 共同作業
- 破壊的な操作や広範囲な変更を行う前には確認を取る。
- 複数の進め方がある場合は、重要な判断理由を説明する。
- 次に作業する人が理解しやすい状態を保つ。

## Git運用
- このマシンで作業を始める前に、`git status` と `git pull --ff-only` で状態を確認する。
- 作業後、ユーザーが別途止めない限り、変更内容を確認して適切なコミットを作成する。
- GitHubへ反映できる状態なら、コミット後に `git push` まで行う。
- コンフリクト、未確認のユーザー変更、認証エラーなどがある場合は、無理に進めず状況を報告して確認を取る。
